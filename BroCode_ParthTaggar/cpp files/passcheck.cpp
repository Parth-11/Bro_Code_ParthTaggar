#include "passcheck.h"
#include "User.h"
void passcheck(const std::string &pass)
{
    // add the conditons for the password
    /*
    1.should have one cap letter
    2.should have a number
    3.should have a symbol
    4.should have atleast 8 characters*/
}